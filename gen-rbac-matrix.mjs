/**
 * يولّد claude/05_RBAC_MATRIX.md من admin/permissions.js.
 *
 *   node gen-rbac-matrix.mjs
 *
 * ============================================================
 * لماذا يوجد هذا الملف
 * ============================================================
 * وثيقة الخصوصية في المرحلة 4 قالت إن «أخطر الحقول لا يختارها أي
 * استعلام إداري»، وكان الكود يختار daily_logs.note. الوثيقة كانت
 * مصيبة في السياسة ومخطئة في وصف الكود، وبقي الفرق سنة.
 *
 * فجدول الصلاحيات لا يُكتب بيد. يُولَّد من المصدر الوحيد
 * (admin/permissions.js) فيستحيل أن يقول غير ما يفعله الخادم.
 *
 * ⚠️ ويُعاد تشغيله بعد **كل** تعديل على المصفوفة. تعديل صلاحية بلا
 * إعادة توليد يترك وثيقة قديمة تبدو صحيحة — وهو بالضبط العطب الذي
 * وُلد هذا الملف ليمنعه.
 *
 * ولا يقرأ قاعدة بيانات ولا يحتاج متغيّرات بيئة: يستورد المصفوفة
 * ويكتب ملفاً. آمن للتشغيل في أي وقت.
 * ============================================================
 */
import { writeFileSync, mkdirSync } from "fs";
import {
  matrixRows, ROLES, ROLE_LABEL, ROLE_DESCRIPTION, permissionsFor, ALL_PERMISSIONS,
} from "./admin/permissions.js";

/* أسماء الموارد بالعربية. المورد الذي لا اسم له يظهر بمفتاحه بدل أن
   يختفي — فإضافة مورد جديد بلا ترجمة تُنقص جمالاً ولا تُخفي صفاً. */
const RES_LABEL = {
  overview: "النظرة العامة", users: "المستخدمون", messages: "الرسائل", content: "المحتوى",
  notifications: "الإشعارات", plans: "الباقات", tax_settings: "البيانات الضريبية",
  billing_settings: "الإعداد المالي", app_settings: "إعدادات التشغيل",
  subscriptions: "الاشتراكات", payments: "المدفوعات", invoices: "الفواتير",
  credit_notes: "الإشعارات الدائنة", refunds: "الاستردادات", billing: "الملف المالي للمستخدم",
  webhooks: "أحداث الدفع", reports: "التقارير", exports: "التصدير",
  admins: "حسابات الإدارة", audit_log: "سجلا التدقيق", break_glass: "الوصول الطارئ",
};

const mark = (v) => (v ? "✅" : "—");
const rows = matrixRows();

let out = `# 05 — مصفوفة الصلاحيات

> **⚠️ هذا الجدول مولَّد من \`admin/permissions.js\` بأمر \`node gen-rbac-matrix.mjs\`، ولا يُحرَّر بيد.**
>
> والسبب مكتوب بثمن: \`04_PRIVACY_CLASSIFICATION.md\` قالت إن «أخطر الحقول
> لا يختارها أي استعلام إداري» وكان الكود يختار \`daily_logs.note\`. جدول
> يُكتب بيد تختلف عن الكود يوماً ما — والفرق يُكتشف بعد سنة.
>
> **أعِد تشغيل الأمر بعد كل تعديل على المصفوفة.**

**عدد الصلاحيات:** ${ALL_PERMISSIONS.length} · **عدد الأدوار:** ${ROLES.length}

---

## 1. الأدوار

| الدور | الاسم | الصلاحيات | الوصف |
|---|---|---:|---|
${ROLES.map((r) => `| \`${r}\` | ${ROLE_LABEL[r]} | ${permissionsFor(r).length} | ${ROLE_DESCRIPTION[r]} |`).join("\n")}

### لماذا ليست سُلَّماً

النموذج السابق كان رقماً واحداً لكل دور:

\`\`\`js
const ROLE_RANK = { support: 1, content_manager: 2, admin: 3, owner: 4 };
requireRole("support")   //  يمرّ كل من رتبته ≥ 1
\`\`\`

وهو يحمل افتراضاً واحداً: أن كل دور أعلى يحتوي كل ما دونه. والافتراض
يسقط فور وجود دور **عرضي** — والمحاسب هو ذلك الدور: يرى الدفاتر كاملة
ولا يقترب من اليوميات. فأين يوضع؟ تحت \`admin\` لا يرى الفواتير، وعنده
يرى البيانات النفسية. **لا موضع صحيح له، لأن السؤال نفسه خطأ.**

### والعطب لم يكن نظرياً

\`content_manager\` رتبته 2 و\`support\` رتبته 1، فكل مسار مكتوب
\`requireRole("support")\` كان يمرّ لمدير المحتوى على الإنتاج:

| المسار | ما كان يراه مدير المحتوى |
|---|---|
| \`GET /admin/billing/subscriptions\` | الاشتراكات والأسعار |
| \`GET /admin/billing/payments\` | كل عملية دفع |
| \`GET /admin/billing/users/:id/billing\` | الملف المالي الكامل لأي مستخدم |
| \`GET /admin/plans\` | الباقات وأسعارها |
| \`GET /admin/overview\` | الإيراد والاسترداد والدفعات الفاشلة |
| \`GET /admin/users\` · \`/admin/messages\` | دليل المستخدمين ورسائلهم |

أي أن اختبار القبول الأول — «Content Manager لا يدخل المحاسبة» — **كان
يفشل**، لا لأن أحداً قرّر ذلك بل لأن رقمين تُقورنا.

---

## 2. المصفوفة الكاملة — Role × Resource × Action

| المورد | الإجراء | ${ROLES.map((r) => ROLE_LABEL[r]).join(" | ")} | الوصف |
|---|---|${ROLES.map(() => ":---:").join("|")}|---|
`;

let lastRes = null;
for (const r of rows) {
  const res = lastRes === r.resource ? "" : (RES_LABEL[r.resource] || r.resource);
  lastRes = r.resource;
  out += `| ${res} | \`${r.action}\` | ${ROLES.map((x) => mark(r.roles[x])).join(" | ")} | ${r.description} |\n`;
}

out += `
---

## 3. دور المحاسب — وقيد قاعدة البيانات الذي حكم شكله

\`admin_users\` أحد الجداول الخمسة عشر التي يملكها المستخدم الفائق
\`postgres\`، وعمود \`role\` عليه:

\`\`\`sql
CHECK (role IN ('support', 'content_manager', 'admin', 'owner'))
\`\`\`

وتوسيعه يحتاج \`ALTER TABLE\` فيرد \`must be owner of relation admin_users\`.

**الحل** هو نفس النمط الذي حلّ به المشروع تعليق الحسابات في الترحيل 003:
حقل جديد لكيان قائم يذهب إلى جدول جديد يملكه التطبيق.

\`\`\`
الدور الفعلي = COALESCE(admin_role_assignments.role, admin_users.role)
\`\`\`

وحساب المحاسب يُكتب في \`admin_users.role\` بقيمة \`support\` — **أضعف
دور يقبله القيد** — وترقيته تأتي من جدول الإسناد وحده. فلو ضاع صف
الإسناد لأي سبب، يسقط الحساب إلى **أقل** صلاحية لا إلى أكثرها.

### ثلاثة قرارات عن المحاسب

1. **صلاحياته كلها قراءة.** §4 تقول «يمكن أن **يرى**
   Payments/Invoices/Refunds/Revenue/Tax» ولا تقول ينفّذها. فالاسترداد
   قرار مدير، وتعديل نسبة الضريبة قرار مالك.
2. **لا يملك \`users:view\`.** الفصل المطلوب ليس «لا يرى الأسماء» —
   الفاتورة النظامية يجب أن تحمل اسم المشتري ويراه في قوائم المالية
   بالضرورة — بل «لا يرى البيانات النفسية». وغياب \`users:view\` يزيد
   أنه لا يتصفّح دليل المستخدمين وجهةً قائمة بذاتها.
3. **ولا \`exports:users\`.** الملف يغادر إلى قرص شخص، فما لا يُعرض على
   شاشة لا يصير ملفاً بالأولى.

---

## 4. ما مُنع عنه \`admin\` — وسبب كل واحد

| الصلاحية | السبب |
|---|---|
| \`admins:*\` | من يدير من يدير كل شيء = قرار ملكية لا تشغيل |
| \`audit_log:view_reads\` · \`view_actions\` | من يُدقَّق عليه لا يقرأ سجل تدقيقه |
| \`tax_settings:edit\` | خطأ فيه يفسد كل فاتورة تصدر بعده — يدخل حرفياً في رمز QR |
| \`billing_settings:edit\` | تغيير النسبة يغيّر كل فاتورة قادمة |
| \`app_settings:edit\` | إعداد يظهر للمستخدم النهائي |
| \`content:bulk_publish\` | نشر 64 عنصراً بضغطة لا يُترك لدور تشغيلي |
| \`break_glass:approve\` | المعتمِد يجب أن يكون غير الطالب — و\`admin\` هو الطالب |
| \`webhooks:view_payload\` | الحمولة الخام قد تحمل تفاصيل وسيلة دفع |
| \`exports:audit\` | تصدير سجل التدقيق يخرجه من حراسة الخادم |

---

## 5. كيف تُفرض — ثلاث طبقات، والثالثة وحدها حماية

| الطبقة | ما تفعله | هل هي حماية؟ |
|---|---|---|
| قائمة اللوحة | تُخفي ما لا تصله صلاحيتك | **لا** — راحة عين |
| \`requirePermission\` في المسار | يردّ 403 قبل تشغيل المعالج | **نعم** |
| الاستعلام نفسه | لا يختار عموداً خارج نطاق الدور | **نعم** — وهي الأقوى |

والطبقة الثالثة هي درس \`daily_logs.note\`: الحقل الذي لا تعرضه أي شاشة
**يظل يعبر الشبكة**، ويستقر في ذاكرة المتصفّح، ويظهر في أي ملف HAR أو
سجل وسيط. ولذلك كتلة الإيراد في \`/admin/overview\` **لا يُشغَّل
استعلامها أصلاً** لمن لا يملك \`overview:view_revenue\` — لا تُخفى في
الواجهة.

### واللوحة لا تحمل مصفوفة خاصة بها

\`GET /admin/auth/me\` يعيد \`permissions: [...]\` محسوبة من هذا الملف،
واللوحة ترسم منها. جدول الرتب في \`admin-src/src/theme.js\` **حُذف** —
كان نسخة ثانية اختلفت عن الأولى فعلاً.

---

## 6. الدور يُقرأ من قاعدة البيانات في كل طلب

\`req.admin.role\` كان يُقرأ من حمولة الرمز، والرمز لقطة لحظة الدخول:

| الفعل | قبل | بعد |
|---|---|---|
| خفض دور مسؤول | يسري بعد 15 دقيقة | **في الطلب التالي** |
| تعطيل حساب | يسري بعد 15 دقيقة | **في الطلب التالي** |
| رمز لحساب غير موجود | **يُقبل** | 401 |
| رمز يدّعي دوراً أعلى | يُقبل بدوره المزعوم | يُقرأ الدور من القاعدة |

الثمن استعلام واحد بمفتاح أوّلي لكل طلب إداري. **ولا تخزين مؤقت
عمداً** — المشروع بلا طبقة Cache، وإدخال واحدة لتوفير استعلام كهذا يعيد
المشكلة نفسها بمدة أقصر.

---

## 7. ما يحرس هذه المصفوفة من الانحراف

| الحارس | الموضع | ما يمنعه |
|---|---|---|
| فحص عند الإقلاع | \`admin/permissions.js\` | خطأ مطبعي في اسم صلاحية → **يُسقط الإقلاع** بدل 403 غامض بعد أسابيع |
| اختبار 11-د | \`test-admin-security.mjs\` | بقاء مستدعٍ لـ\`requireRole\` |
| اختبار 11-هـ | \`test-admin-security.mjs\` | مسار إداري بلا \`requirePermission\` |
| اختبار 11-و | \`test-admin-security.mjs\` | استثناء يصير غطاءً على غياب الحارس |
| \`gen-rbac-matrix.mjs\` | جذر المستودع | اختلاف الوثيقة عن الكود |

---

## 8. المتبقي — بصراحة

**\`REVOKE UPDATE, DELETE\` على سجلَي التدقيق غير منفَّذ** (البند 12.19).
لا يوجد في المستودع كله كود يعدّل أو يحذف منهما، لكن «لا يوجد كود
يفعلها» ليس «لا يمكن أن تُفعل». والتشديد الحقيقي يحتاج حساب \`postgres\`
لأنه مالك \`admin_access_log\`؛ وأما \`admin_action_log\` فيملكه
\`kanaf_adel\` نفسه — **ومن يملك الجدول يقدر يعيد منح نفسه الصلاحية،
فالسحب الذاتي لا يعني شيئاً أمنياً.**

**حدّ محاولات الدخول بالعنوان لا بالحساب.** القفل لكل حساب يحتاج عموداً
على \`admin_users\` وهو ممنوع، وجدولاً جانبياً لهذا وحده لا يستحق اليوم:
كلمات مرور الإدارة بحدّ أدنى 15 حرفاً، وعشر محاولات كل ربع ساعة لكل
عنوان تجعل التخمين المتصل غير عملي. **قيد معروف لا ثغرة مجهولة.**
`;

mkdirSync("claude", { recursive: true });
writeFileSync("claude/05_RBAC_MATRIX.md", out);
console.log(
  `✅ claude/05_RBAC_MATRIX.md — ${out.split("\n").length} سطر · ` +
  `${ALL_PERMISSIONS.length} صلاحية × ${ROLES.length} أدوار`
);
